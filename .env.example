# Copy this file to .env and fill in your own values.

# Required for LLM-based planning (planner.py). Without this, the agent
# still runs, but falls back to a fixed generic subgoal template instead of
# dynamically decomposing your specific objective.
# Get a free key at https://console.groq.com/keys
GROQ_API_KEY=
